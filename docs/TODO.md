# Life Game — Todo

## Done

- [x] **UI (заморожено)**: TopBar (цикл, расы, зум, ресурсы), RightPanel (настройки, старт/рестарт), GameBoard (canvas)
- [x] **Типы данных**: `ResourceType` (9 типов), `CellType` (base, resource, fabric, shadowed), `CellData`, `RaceData`, `MapMeta`
- [x] **Генерация карты**: 2D массив `width × height`, взвешенный рандом ресурсов, пустые клетки → SAND, базы не на краю
- [x] **Расы**: расстановка с min дистанцией 4, цвета (`RACE_COLORS`), иконки баз (mdi-home) в цвет расы
- [x] **Хранение (localStorage)**: хэш 16 hex, ключи `${hash}-${x}-${y}` / `${hash}-meta`, очистка старого хэша
- [x] **Canvas-рендер**: иконки через Iconify API, тултип при ховере, подсветка захваченных клеток (tint + border)
- [x] **Циклы**: авто-ход раз в секунду, пауза/продолжить, сохранение после каждого цикла
- [x] **Экономика**:
    - Сбор ресурсов с контролируемых клеток (RESOURCE_YIELDS)
    - Содержание расы за цикл (RACE_MAINTENANCE: meal 3, water 2, material 1)
    - Истощение ресурсов (depletion растёт с циклами)
    - Восстановление истощённых клеток через 10 циклов
    - При нехватке ресурсов — потеря 1 клетки с края territory за цикл (приоритет: истощённые → нормальные → fabric → base)
    - Раса проигрывает при потере последней базы
- [x] **Захват клеток**: ИИ рас (захват по стоимости из таблицы, прогресс-бар на канвасе, пунктир на цели)
- [x] **Фабрики**: AI строит на controlled клетках (-1 material/цикл), иконка фабрики, прогресс-бар, сбор ресурсов только через фабрики
- [x] **Приоритеты рас**: каждая раса получает случайные веса (expansion, building, war, reinforcement), AI действует по весам
- [x] **Бой**: атака на вражеские клетки (стоимость 1 material/цикл), прогресс 5 циклов, при захвате клетка переходит атакующему, атакующий теряет все клетки при потере базы (приоритет целей: база > фабрика > ресурсы)
- [x] **Победа/поражение**: когда остаётся 1 раса — победа, 0 — ничья, таймер останавливается, показывается оверлей с кнопкой «Новая игра»